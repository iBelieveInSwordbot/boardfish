node_modules/
dist/
.DS_Store
*.log

# TLS certs (self-signed for HTTPS listener). Never commit private keys.
tls/*.crt
tls/*.key
tls/openssl.cnf

# Runtime storage: server-side content-addressed media cache, ai-proxy
# temp scratch dirs. All regenerable.
data/
ai-proxy/tmp-llm/

