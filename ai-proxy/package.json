{
  "name": "boardfish-ai-proxy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Local Node bridge from Boardfish 4 web UI to OpenClaw (Ronan for shot lists, Nano Banana for panel images).",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
